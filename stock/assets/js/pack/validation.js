/**
 * Pack Validation Module
 * @module pack/validation
 */
(function(window) {
    'use strict';
    const PackValidation = {
        init(config) { console.log('[PackValidation] Initialized'); },
        validateQuantity(input, value) { /* TODO */ },
    };
    window.PackValidation = PackValidation;
})(window);
