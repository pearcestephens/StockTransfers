/**
 * Pack Shipping Calculator Module
 * @module pack/shipping
 */
(function(window) {
    'use strict';
    const PackShipping = {
        init(config) { console.log('[PackShipping] Initialized'); },
    };
    window.PackShipping = PackShipping;
})(window);
